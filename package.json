{
  "name": "@toruslabs/e2e-tests",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test:browser": "playwright test --headed",
    "test": "playwright test",
    "trace": "playwright show-trace",
    "install-deps": "playwright install-deps",
    "test:build": "docker build -t test-image .",
    "test:docker": "docker run -it test-image /bin/bash"
  },
  "dependencies": {
    "@playwright/test": "^1.30.0",
    "bip39": "^3.0.4",
    "dotenv": "^16.0.3",
    "generate-password": "^1.7.0",
    "mailosaur": "^8.5.0",
    "playwright": "^1.30.0",
    "serve-handler": "^6.1.3"
  },
  "devDependencies": {
    "@tsconfig/node16": "^1.0.3",
    "@types/node": "^16",
    "@types/serve-handler": "^6.1.1",
    "lighthouse": "^9.6.3",
    "prettier": "^2.7.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  }
}
